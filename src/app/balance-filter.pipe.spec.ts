import { BalanceFilterPipe } from './balance-filter.pipe';

describe('BalanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BalanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
